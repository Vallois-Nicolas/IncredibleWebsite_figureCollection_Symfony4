{{ form_start(form) }}
<div class="row">
    <div class="col-12">
        {{ form_row(form.name) }}
        <button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
    </div>
</div>
{{ form_end(form) }}